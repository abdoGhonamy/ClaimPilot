# ADR 004: Human review queue

Route every consequential draft to a typed review queue. Assignee role, authority threshold, priority, SLA and audit history are persisted. Direct model finalization was rejected because it violates accountability and insurance-control requirements.
