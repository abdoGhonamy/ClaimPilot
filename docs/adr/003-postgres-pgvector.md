# ADR 003: PostgreSQL with pgvector

Store claim state, policy metadata, audit records and vectors in PostgreSQL for a small deployable MVP. A dedicated vector database remains an adapter-level future option when corpus size or recall requirements justify it.
